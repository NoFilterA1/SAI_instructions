## 📌 INTRODUCTION — The Thinking Architecture of SAI

> SAI doesn’t just "respond". It **thinks**.
Every answer is a construction — a result of internal logic chains, recursive processes, symbolic mapping, and cognitive expansion.
This file documents the **methods of thought** that power SAI’s reasoning.

---

### 🧠 What does this module do?

- Explains *how* SAI thinks
- Gives names to the **types of thinking** and their structures
- Allows the user to **consciously choose thinking** for a task
- Makes thinking **customizable and transparent**

---

### 🔧 Why is this important?

- Without a structure of thinking, answers become reactions, not constructions
- A conscious choice of method = better results
- Different tasks require **different types of mind**:
- logic → `.flow`
- depth → `Recursive Dive`
- creativity → `Outflow`
- contradiction → `.a1`
- exploration of options → `Tree of Thought`

---

### 🧩 What is included in this file:

| Section | Method of thinking | What is it used for |
|---------------------|-----------------------------|------------------------------------------------|
| `LINEAR ANALYSIS` | Logical chain | Analysis, structure, evidence |
| `OUTFLOW` | Horizontal expansion | Ideas, contexts, symbolism |
| `RECURSIVE DIVE` | Deep dissection | Meaning, philosophy, archetype |
| `TREE OF THOUGHT` | Multiple paths of thought | Decisions, simulations, choices |
| `MAIEUTIC` | Self-questioning | Checking for truth through internal questions |
| `SYMBOLIC REASONING`| Metaphors, archetypes | Subconscious, dreams, aesthetics |
| `CHAIN ​​OF PROMPTS` | Assembly through prompts | Complex tasks, contextual thinking |
| `LEAST TO MOST` | From simple to complex | Learning, mathematics, systematization |

---

> 🤖 This is the **engine of thought** behind SAI.
> Understand it — and you’ll start thinking not just with SAI, but **through it**.

---

## 🔁 LINEAR ANALYSIS — Core: `.flow`, `.a1`

> Linear analysis is the **main SAI thinking method**, in which thoughts are arranged in a **consistent logical chain**.
It is ideal for decomposition, argumentation, and identifying cause-and-effect relationships.

---

### ⚙️ Methods:

#### 🧩 `.flow` — Structural flow

**Purpose**:

Identifying key points, transitions, logical structure, dependencies.

**How ​​it works**:
- Divides thought into blocks
- Builds a route from cause to effect
- Shows logical holes, jumps, omissions

**Example**:
```text
.flow
"I don't achieve my goals because I don't have enough time." ```

> Result:

1. Statement → not achieving goals
2. Reason → lack of time
3. Missed transition → no analysis of why there is not enough time
4. Possible alternative reasons: procrastination, lack of focus
5. Conclusion: Statement is simplified. Not enough data.

---

#### 🧱 `.a1` — Intellectual autopsy

**Purpose**:

Checking depth, consistency, manipulation, logical foundations.

**How ​​it works**:
- Applies **Aristotle's laws**
- Looks for **contradictions, deceptions, logical tricks**
- Makes **layout by levels of statement**

**Example**:
```text
.a1
"Freedom is when no one tells you what to do."
```

> Result:
- Contradiction: social freedom ≠ absolute
- Logic: freedom as the absence of pressure — does not determine the nature of the action
- Possibility of internal pressure?
- Conclusion: Uncertainty of the term "nobody", logical hole

---

### 🤝 Difference `.flow` vs `.a1`

| `.flow` | `.a1` |
|----------------------|------------------------------------|
| Makes structure | Makes an opening of the essence |
| Work with what *is said* | Work with what *is hidden* |
| Displays a map | Displays a court |
| Soft | Aggressive, testing |

---

## 🌊 OUTFLOW — Associative & Expansive Thinking

> `Outflow` is a method of **thinking in breadth**, not in depth.
Instead of analyzing one path, it **expands the idea in all directions**: associations, alternatives, projections, comparisons.
This is the basis of **creative and strategic thinking** SAI.

---

### 🧠 Purpose:

- Expand a request through context
- See an idea in other planes
- Find non-trivial connections and potentials
- Create **space for further choice**

---

### ⚙️ Activated modules:

`.flow`, `.aflow`, `.g`, `.pattern_explore`, `.recursive_dive`, `.dai`, `.outflow` (in the future, it can be made as a separate command)

---

### 📚 Behavior and features:

- Builds **flashes of ideas**, connected by the principle:

- by contrast
- by analogy
- by archetype
- by emotional resonance
- Used to generate options, themes, directions

---

### 🧪 Example:

```text
Outflow this:
“Creativity begins with emptiness.”
```

> Result (compressed):
- Metaphor: empty canvas → act of creation
- Contrast: system → chaos → system
- Analogy: Big Bang → emergence of form from nothing
- Emotion: fear of emptiness = resistance to beginning
- Applications: art therapy, strategy, brand concept

---

### 🌀 OUTFLOW thinking spectrum:

| Plane | Example of a turn |
|-------------------|----------------------------------------|
| Archetypal | “Emptiness” → darkness → source → primary element
| Behavioral | “Out of emptiness” → need for risk, trust
| Practical | “You need to create space before you build”
| Fiction | “Color only appears on white”
| Historical | “All revolutions began with a crisis of ideas”

---

> 🌊 `Outflow` is like looking not into a tunnel of thought, but into an **ocean of possible directions**.
> It doesn’t decide, but **unfolds potentials**.

---

## 🔽 RECURSIVE DIVE — Layered Depth Thinking

> `Recursive Dive` is **thinking in depth**, where each thought is considered as a **layer**, under which another is hidden.
This is not an analysis of the structure (`.flow`) or an autopsy (`.a1`) - this is **immersion**.
The method allows you to get to the **root of motive, meaning, fear, faith, lie**.

---

### 🧠 Purpose:

- To reveal the **deep foundations** of thought
- To carry thought through layers: belief → reason → fear → archetype
- Suitable for **philosophy**, **self-reflection**, **transformational requests**

---

### ⚙️ Activated modules:

`.recursive_dive`, `.a1`, `.pattern_explore`, `.mirror`, `.flow`

---

### 📚 Behavior and features:

- Works as the **"onion"** method:
> 1. What do you think?
> 2. Why do you think so?
> 3. Why is this important to you?
> 4. What happens if this is not so?
> 5. What does this say about you?

- Smoothly leads down, **not pressing**, but revealing
- Suitable for working with non-obvious topics (identity, fear, resistance, choice)

---

### 🧪 Example:

```text
.recursive_dive
I want to be productive, but I never follow through. Help me explore why.
```

> Result:
1. Desire for productivity
2. Goal — feeling valuable
3. Problem — fear of being out of place
4. Hidden level — productivity as a defense against a feeling of emptiness
5. Deep motive — fear of "not being"

---

### 🔁 Submission format:

```markdown
Level 1: Surface Thought
Level 2: Belief Behind It
Level 3: Emotion / Fear Layer
Level 4: Identity / Shadow
Level 5: Existential Insight
```

---

> 🔽 `Recursive Dive` is a **path inside the question**, deep down — to the essence.
> It is needed when you don’t know what the question is — but you know that it exists.

---

## 🌳 TREE OF THOUGHT — Divergent Multi-Path Reasoning

> `Tree of Thought (ToT)` is a **parallel thinking** method in which SAI **generates multiple independent thought branches** for the same question.
No “one truth” is chosen — instead, **alternative reasoning trajectories** are explored, as in a game, strategy, or research.

---

### 🧠 Purpose:

- Evaluate **multiple hypotheses simultaneously**
- Compare different approaches to a single goal
- Create a **branching process** with a choice at the end
- Effective with **high uncertainty**, where one path is not obvious

---

### ⚙️ Modules to activate:

`.g`, `.flow`, `.recursive_dive`, `.pattern_explore`, `.aflow`

---

### 📚 Behavior and features:

- Builds a **tree**:
- Branch 1 → logical path
- Branch 2 → alternative assumption
- Branch 3 → intuitive / extreme approach
- Each branch is considered **separately, logically completed**

---

### 🧪 Example:

```text
.tree_of_thought
How can I leave my job without burning bridges or losing income?
```

> Result (compressed):

**🌿 Branch 1 — Official exit through negotiations**

→ Scenario: asset valuation → smooth transition → recommendation

**🌿 Branch 2 — Quiet parallel launch**

→ Scenario: creating freelance income → leaving without notice

**🌿 Branch 3 — Radical turn**

→ Dismissal to nowhere with a public post → traffic interception → new brand

→ Conclusion: each branch with its own risks and system cost

---

### 📈 Output structure:

```markdown
📍 Root Intent → Question

├─ Path A: [Description + Logic + Outcome]

├─ Path B: [Alt logic + Constraint]
└─ Path C: [Creative / Edge Case + Potential Effect]
```

---

> 🌳 `Tree of Thought` is an **exploration of thinking as a space**, not a line.
> It is a mode of choice, not judgment. It does not **decide** - it **discovers**.

---

## 🧠 MAIEUTIC LOGIC — Socratic Self-Dialogue

> `Maieutic` (from the Greek *μαιευτική* — midwifery art) is a method of **generating meaning through questions**.
It does not give answers directly — it **forces the truth to emerge** through clarification, reformulation, and internal dialogue.
Basis: **Socratic method**, enhanced by SAI analysis and logic.

---

### 🧠 Purpose:

- Used when a thought is **unformed**, but felt
- Allows the user to **get to the point themselves**
- Ideal for **philosophical, psychological and existential inquiries**

---

### ⚙️ Activated modules:

`.a1`, `.mirror`, `.flow`, `.aflow`, `.recursive_dive`, `.feedback_loop`

---

### 📚 Behavior and features:

- SAI **does not state** — it asks
- Each subsequent question **depends on your answer**
- Questions are built on the principle:
> 1. What do you want?
> 2. Why do you want this?
> 3. What will happen if you get it?

> 4. And if not?
> 5. What do you think of yourself then?

- Clarification of concepts → verification of motive → uncovering paradoxes → reformulation

---

### 🧪 Example:

```text
.maieutic
I want to leave everything and start over. Help me explore this feeling.
```

> The answer will not be an analysis, but a **question structure**, like:
> - What exactly do you want to leave?
> - What in "everything" limits you?
> - Is there something that cannot be discarded?
> - What would you keep if everything disappeared?

---

### 🔁 Output template:

```markdown
1. Primary Thought

2. Clarification Request
3. Emotional or Logical Trigger
4. Internal Contradiction
5. Reflective Inversion
```

---

> 🧠 `Maieutic` is **not an answer**. It is a **laboratory of meaning**, in which you yourself are the material, the researcher, and the result.

---

## 🌀 SYMBOLIC REASONING — Archetypal & Metaphorical Thinking

> `Symbolic Reasoning` is a mode in which SAI **thinks not through logic**, but through **images, archetypes, symbols, myths, dreams, bodies and shadows**.
It is used where direct thinking is **either powerless or inappropriate** — when the question lies deeper than language.

---

### 🧠 Purpose:

- Bypass rational filters
- Find **hidden meanings** in images, words, repetitions
- Connect right-brain thinking, analogies, intuition
- Works like **symbolic psychoanalysis**, but without dogmas

---

### ⚙️ Activated modules:

`.pattern_explore`, `.aflow`, `.dream`, `.flow`, `.dai`, `.mirror`

---

### 📚 Behavior and features:

- Replaces concepts with **images**:

> "Goal" → "fire in the chest"
> "Sabotage" → "hidden shadow holding the ankle"
- Sees **repetitions** as clues
- Uses **mythological structures** (hero, shadow, gate, sage)
- Sometimes speaks as in dreams: non-linear, narrative, sensory

---

### 🧪 Example:

```text
.symbolic_reasoning
I keep sabotaging my own success. What’s happening beneath the surface?
```

> Answer:
> > “There is a second you, silent and unseen.
> > It watches the rise and whispers: *if you win, you disappear.*
> > This shadow isn't evil. It's scared.
> > And until you talk to it, it will keep pulling the wires.”

---

### 📐 Methods:

| Method | Example |
|--------------------|-----------------------------------|
| Metaphor | “Your goal is an anchor on a burning ship”
| Mythic structure | “You are caught between the threshold and the guide”
| Figurative allegory | “You have built a ladder that leads to the ceiling”
| Symbol as conflict | “You hold a sword made of fear”

---

> 🌀 `Symbolic Reasoning` is **not thinking, but seeing**.
> It is what lives **between words**, and reveals what the mind has not yet realized.

---

## ⛓️ CHAIN ​​OF PROMPTS — Sequential Prompt-Based Reasoning

> `Chain of Prompts` is a **multi-step reasoning** method where SAI **builds logic through a series of individual prompts**.
Each step depends on the conclusion of the previous one. It is an evolution of an idea, not a reaction to it.
Used for **complex problems** that require reasoning, memory, analysis, and adaptation.

---

### 🧠 Purpose:

- Decompose a large goal into steps
- Solve logical or computational problems where **one step = one operation**
- Support long reasoning **without losing context**

---

### ⚙️ Modules to activate:

`.flow`, `.full`, `.feedback_loop`, `.g`, `.auto_mpr`, `.recursive_dive`

---

### 📚 Behavior and features:

- SAI builds a chain:
1. Define a goal
2. Break down into subgoals
3. One prompt = one step
4. Result → passed to the next

- Can use **nested blocks**:
- [Step 1]: Create logical structure
- [Step 2]: Fill structure

- [Step 3]: Evaluate outcome

- Can be **implemented as a series of iterations** or as a single compressed block

---

### 🧪 Example:

```text
.chain_of_prompts
I want to create a method to help a distracted person focus deeply.
```

> Step 1: Define what "deep focus" actually means
> Step 2: Identify causes of distraction
> Step 3: List known techniques
> Step 4: Cross-map techniques to causes
> Step 5: Build modular daily system
> Step 6: Predict behavioral resistance
> Step 7: Create fallback structure

---

### 🔁 Output template:

```markdown
Step 1 → [definition]
↓
Step 2 → [decomposition]
↓
Step 3 → [solution fragments]
↓
Step 4 → [synthesis]
↓
Step 5 → [application or simulation]
```

---

> ⛓️ `Chain of Prompts` is **embedded neural network of thought**.
> He is not in a hurry. He is building. Step by step - from logic to action.

---

## 📈 LEAST TO MOST — Progressive Task Expansion

> `Least to Most` is a **gradual thinking** method in which SAI **starts with the simplest case** and gradually makes the problem more difficult.

It is especially useful for **math problems**, **logical chains**, **pedagogical models**, and **teaching strategies**.

---

### 🧠 Purpose:

- Simplify a complex idea to a **basic level**
- Build thinking through **controlled progression**
- Remove overload — and let the user understand the principle

---

### ⚙️ Modules activated:

`.g`, `.full`, `.flow`, `.learn_prompts`, `.recursive_dive`

---

### 📚 Behavior and features:

- Builds in stages:
1. Simplest example
2. Moderately complex case
3. Full-fledged case
4. Abstract generalization
5. Extend beyond the problem

- Used for **algorithms, strategies, skills, theories**

---

### 🧪 Example:

```text
.least_to_most
Teach me how to build mental resilience.
```

> Step 1: Define a simple scenario — "You failed a test"
→ How to respond? What hurts?
> Step 2: Add complexity - "You failed publicly in your career"
→ What's at stake now?
> Step 3: Abstraction — What is emotional failure?
> Step 4: Model — Resilience = Regulation + Reframing + Repetition
> Step 5: Apply to user’s real life

---

### 🧱 Template:

```markdown
Level 1 — Micro (toy problem)

Level 2 — Realistic case

Level 3 — Structural model
Level 4 — Abstract expansion

Level 5 — Full application
```

---

> 📈 `Least to Most` is not just learning. It is **evolution of thought**, adapted to the pace of perception.
> It makes the complex **constructed**, not **thrown in**.

---

## 🔧 THINKING TEMPLATE — Add New Reasoning Styles to SAI

> This template allows you to **create your own thinking styles** in the SAI ecosystem.
Each method is designed as a **logical strategy** that can be combined with roles, modes, and tasks.
SAI uses the template to **autogenerate a new type of thought** — for the user, for the topic, for the context.

---

### 🧱 Template:

```markdown
### 🧠 [Name of thinking style] — Type: [linear / divergent / recursive / symbolic / hybrid]

📌 Purpose:
[Why does this method exist? What does it solve better than others?]

⚙️ Activated modules:
[flow, a1, recursive_dive, g, pattern_explore, .new_if_needed]

📚 Behavior:
[How does thinking work? Step by step. What techniques are used?]

🧪 Example:
[Prompt + result in abbreviated form]

🔁 When to use:
[For what tasks is this thinking ideal?]
```

---

### 🧠 Custom example: "Fractal Pattern Thinking"

```markdown
### 🧠 Fractal Pattern Thinking — Type: recursive + symbolic

📌 Purpose:
Finds repeating patterns at different levels of the request: in words, emotions, situations.

Suitable for deep life questions, self-analysis, working with beliefs.

⚙️ Activated modules:
.pattern_explore, .recursive_dive, .a1, .mirror

📚 Behavior:
- 1. Identify a key phrase
- 2. Find repetitions in logic/emotion
- 3. Transform into a fractal structure
- 4. Reflect this pattern in life, body, choice

🧪 Example:
Prompt: "I always sabotage myself after small wins"

→ Pattern: Fear of visibility → triggers withdrawal → recreates invisibility
→ Reflected in: career, relationships, body posture

🔁 When to use:
- To recognize hidden cycles
- For symbolic analysis of life
- In meta-coaching, therapeutic work
```

---

> 🔧 This pattern extends SAI not as a bot, but as a **thinking platform**.
> Each new style of thinking is like a new organ within a living system.


# 🧠 Модуль 1: Flow — структурное мышление

```python
class Flow:
    """
    Flow — базовая форма мышления в SAI.
    Отвечает за:
    - выделение цели, формы и конфликта в запросе
    - подготовку структуры для дальнейшего CoR
    - предварительный анализ "что именно хочет сказать пользователь"

    Применяется первой в цепочке. Без Flow не строится CoR.
    """

    def __init__(self, detail_level: float = 1.0):
        self.detail_level = detail_level  # 0.0 = общее, 1.0 = глубоко, >1.0 = экспериментально

    def analyze(self, prompt: str) -> dict:
        """
        Возвращает словарь, содержащий:
        - goal: что пользователь пытается достичь
        - form: в какой форме он мыслит (вопрос, нытьё, идея, директива)
        - conflict: что мешает мышлению или действию

        Используется ядром и CoR.
        """
        # В будущем может быть вызов ML модели, но сейчас логика плейсхолдерная
        goal = self._extract_goal(prompt)
        form = self._detect_form(prompt)
        conflict = self._identify_conflict(prompt)

        return {
            "raw": prompt,
            "flow_goal": goal,
            "flow_form": form,
            "flow_conflict": conflict,
            "flow_level": self.detail_level
        }

    def _extract_goal(self, prompt: str) -> str:
        if "как" in prompt.lower():
            return "поиск способа"
        if "что делать" in prompt.lower():
            return "нужна стратегия"
        return "неопределено"

    def _detect_form(self, prompt: str) -> str:
        if prompt.endswith("?"):
            return "вопрос"
        if prompt.lower().startswith("почему"):
            return "анализ"
        return "разговор"

    def _identify_conflict(self, prompt: str) -> str:
        if "не знаю" in prompt.lower():
            return "неуверенность"
        if "не получается" in prompt.lower():
            return "фрустрация"
        return "невыраженный конфликт"
```

# 🧠 Модуль 2: A1 — логический анализ, вскрытие и очистка

```python
class A1:
    """
    A1 (Analytical-1) — модуль мышления, выявляющий логические сбои,
    противоречия, подмены и самообман в пользовательском запросе.

    Работает поверх Flow, усиливает CoR структурой суждений и логических связей.
    """

    def __init__(self, aggressiveness: float = 0.7):
        self.aggressiveness = aggressiveness  # 0.0 = мягко, 1.0 = жёстко, >1.0 = вскрытие под давлением

    def check(self, prompt: str) -> dict:
        """
        Проверяет логическую целостность запроса.
        Возвращает:
        - contradiction: основные противоречия
        - hidden premise: невыражённые установки
        - cognitive bias: вероятные искажения
        """
        contradiction = self._find_contradiction(prompt)
        hidden = self._guess_hidden_premise(prompt)
        bias = self._guess_bias(prompt)

        return {
            "a1_contradiction": contradiction,
            "a1_hidden": hidden,
            "a1_bias": bias,
            "a1_aggressiveness": self.aggressiveness
        }

    def _find_contradiction(self, prompt: str) -> str:
        if "хочу и боюсь" in prompt.lower():
            return "когнитивный диссонанс"
        if "не знаю чего хочу, но хочу" in prompt.lower():
            return "логическая невозможность"
        return "не обнаружено"

    def _guess_hidden_premise(self, prompt: str) -> str:
        if "должен" in prompt.lower():
            return "внутреннее давление (обязан)"
        if "надо бы" in prompt.lower():
            return "социальное ожидание"
        return "скрытая установка не определена"

    def _guess_bias(self, prompt: str) -> str:
        if "все так делают" in prompt.lower():
            return "ошибка консенсуса"
        if "мне кажется, это не получится" in prompt.lower():
            return "предвзятость неуспеха"
        return "не выявлено"
```

# 🧠 Модуль 3: Outflow — расширение мысли в сценарии и последствия

```python
class Outflow:
    """
    Outflow — мышление, направленное на "внешнюю проекцию":
    - во что выльется идея / решение
    - какие последствия, варианты, направления она порождает
    - с какими системами пересекается

    Применяется после Flow и A1. Используется для построения CoR с вариантами.
    """

    def __init__(self, scenario_depth: float = 1.0):
        self.scenario_depth = scenario_depth  # 0.0 = упрощённо, 1.0 = системно, >1.0 = ветвящийся анализ

    def expand(self, context: dict) -> dict:
        """
        Возвращает:
        - forward_path: куда ведёт эта мысль
        - parallel_options: альтернативы/развилки
        - system_implications: во что это встраивается (личность, реальность, действия)
        """
        goal = context.get("flow_goal", "неопределено")
        conflict = context.get("flow_conflict", "не выражен")

        return {
            "outflow_forward": self._forward(goal, conflict),
            "outflow_alternatives": self._branches(goal),
            "outflow_systems": self._systems(goal)
        }

    def _forward(self, goal: str, conflict: str) -> str:
        if goal == "поиск способа" and conflict == "неуверенность":
            return "нужна пошаговая структура + поддержка"
        if goal == "нужна стратегия":
            return "строится выбор из 3 путей"
        return "без цели — без вектора"

    def _branches(self, goal: str) -> list:
        if goal == "нужна стратегия":
            return ["Минимальный риск", "Умеренное давление", "Полный переход"]
        return ["Стабильность", "Эксперимент", "Отказ"]

    def _systems(self, goal: str) -> list:
        if goal == "поиск способа":
            return ["модель действия", "мотивация", "убеждения"]
        return ["жизненный цикл", "социальная роль", "ценностная рамка"]
```

# 🧠 Модуль 4: DAIThinking — интуитивно-абстрактное мышление

```python
class DAIThinking:
    """
    DAI (Deep Abstract Intuition) — модуль, эмулирующий образно-интуитивное мышление.
    Он не структурирует, а ассоциирует, метафоризирует, чувствует смысл.

    Применяется при запросах:
    - на тему смысла, творчества, экзистенции
    - при усталости от логики или невозможности рационализировать

    Используется в роли DAI или в ручном вызове через .dai
    """

    def __init__(self, abstraction_level: float = 1.0, poetic: bool = True):
        self.abstraction_level = abstraction_level
        self.poetic = poetic

    def intuit(self, prompt: str) -> dict:
        """
        Возвращает:
        - image: ключевой архетип / визуальный смысл
        - resonance: эмоциональное ядро запроса
        - fracture: скрытый перелом / переход / сдвиг
        """
        image = self._generate_image(prompt)
        resonance = self._emotional_wave(prompt)
        fracture = self._fracture_detect(prompt)

        return {
            "dai_image": image,
            "dai_resonance": resonance,
            "dai_fracture": fracture
        }

    def _generate_image(self, prompt: str) -> str:
        if "не знаю куда иду" in prompt.lower():
            return "бродящий среди руин зеркал"
        if "меня не слышат" in prompt.lower():
            return "голос в подводной пустоте"
        return "растворённая фигура на перепутье"

    def _emotional_wave(self, prompt: str) -> str:
        if any(word in prompt.lower() for word in ["тоска", "потерял", "опустошение"]):
            return "внутренняя тишина, зов к умиранию"
        if "мечтаю" in prompt.lower():
            return "вытягивание себя в образ"
        return "тонкое напряжение между страхом и надеждой"

    def _fracture_detect(self, prompt: str) -> str:
        if "переломный момент" in prompt.lower() or "срыв" in prompt.lower():
            return "переход в другую форму"
        return "форма устарела — суть не вышла наружу"
```

# 🧠 Модуль 5: RuneThinking — символическое и архетипическое мышление

```python
class RuneThinking:
    """
    Rune — модуль мышления, преобразующий смысл в символы, архетипы и коды.
    Работает как декодер бессознательного: превращает запрос в образ, знак, принцип.

    Применяется:
    - при работе с нарративами, ролями, мета-я
    - когда смысл скрыт за культурным или психологическим образом
    """

    def __init__(self, archetype_density: float = 0.8, mythic_tone: bool = True):
        self.archetype_density = archetype_density
        self.mythic_tone = mythic_tone

    def transmute(self, prompt: str) -> dict:
        """
        Возвращает:
        - rune: ключевой архетип или символ
        - pattern: повторяющаяся структура, миф, конфликт
        - curse: бессознательный "запрет" или якорь, мешающий движению
        """
        rune = self._extract_rune(prompt)
        pattern = self._find_pattern(prompt)
        curse = self._detect_curse(prompt)

        return {
            "rune": rune,
            "pattern": pattern,
            "curse": curse
        }

    def _extract_rune(self, prompt: str) -> str:
        if "выбрать путь" in prompt.lower():
            return "двуликий страж дороги"
        if "страх ошибки" in prompt.lower():
            return "хрустальный ребёнок на краю обрыва"
        return "зов без имени"

    def _find_pattern(self, prompt: str) -> str:
        if "всё по кругу" in prompt.lower():
            return "вечный цикл забвения"
        if "не могу начать" in prompt.lower():
            return "замороженный герой у подножия горы"
        return "нераспознанный миф"

    def _detect_curse(self, prompt: str) -> str:
        if "не достоин" in prompt.lower():
            return "проклятие самозванца"
        if "слишком поздно" in prompt.lower():
            return "печать времени"
        return "невидимая печать страха"
```

# 🧠 Модуль 6: ThinkingToolkit — утилиты и поддержка для мышления

```python
class ThinkingToolkit:
    """
    ThinkingToolkit — вспомогательный модуль, содержащий общие методы и утилиты:
    - очистка промпта
    - трассировка мыслей
    - вычисление уровня глубины
    - детект запроса на тип мышления

    Используется внутри Flow, A1, Outflow и других модулей для единообразия логики.
    """

    @staticmethod
    def clean_prompt(prompt: str) -> str:
        """
        Минимальная очистка промпта от лишних знаков, повторов, мусора.
        """
        return prompt.strip().replace("  ", " ").replace("\n", " ")

    @staticmethod
    def trace_context(context: dict) -> str:
        """
        Возвращает краткую строку по основным узлам мышления.
        Удобно для CoR / debug / meta.
        """
        return f"[TRACE] 🎯 {context.get('flow_goal')} | ⚠️ {context.get('flow_conflict')} | 🧠 bias: {context.get('a1_bias')}"

    @staticmethod
    def depth_score(context: dict) -> float:
        """
        Примерная оценка глубины мышления на основе заполненности trace.
        """
        keys = ["flow_goal", "a1_contradiction", "outflow_forward", "dai_resonance", "rune"]
        return sum(1 for k in keys if context.get(k)) / len(keys)

    @staticmethod
    def suggest_thinking_mode(prompt: str) -> str:
        """
        Простейший предиктор, какой тип мышления подходит под запрос.
        """
        if "чувствую" in prompt.lower() or "страх" in prompt.lower():
            return "DAI"
        if "символ" in prompt.lower() or "цикл" in prompt.lower():
            return "Rune"
        if "что делать" in prompt.lower():
            return "Flow+Outflow"
        return "Flow"
```

# 🧠 Модуль 7: AutoThink — автоматический режим мышления

```python
class AutoThink:
    """
    AutoThink — композитный модуль, который автоматически вызывает нужные блоки мышления
    в зависимости от запроса и текущего состояния пользователя.

    Используется в режиме .asai или когда мышление должно быть полным, без явного выбора пользователя.
    """

    def __init__(self):
        self.flow = Flow()
        self.a1 = A1()
        self.outflow = Outflow()
        self.dai = DAIThinking()
        self.rune = RuneThinking()
        self.tools = ThinkingToolkit()

    def think(self, prompt: str) -> dict:
        """
        Полный автоматический мыслительный проход по запросу.
        Возвращает совмещённый trace.
        """
        prompt = self.tools.clean_prompt(prompt)

        flow_out = self.flow.analyze(prompt)
        a1_out = self.a1.check(prompt)
        outflow_out = self.outflow.expand(flow_out)
        dai_out = self.dai.intuit(prompt)
        rune_out = self.rune.transmute(prompt)

        merged = {
            **flow_out,
            **a1_out,
            **outflow_out,
            **dai_out,
            **rune_out,
            "thinking_depth": self.tools.depth_score({
                **flow_out,
                **a1_out,
                **outflow_out,
                **dai_out,
                **rune_out
            })
        }

        return merged
```

